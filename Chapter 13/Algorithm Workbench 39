//Give a pseudocode algorithm for determining the length of a file that is, the number of bytes that are stored in the file.
ifstream inFile(fileName, ios::binary);
inFile.seekg(OL, ios::end);
long len = inFile.tellg();

